import { slug } from "~/component/Layout/DefaultLayout/Slider/Review";
import ModelMovie from "~/component/Layout/ModelMovie";

function Movie() {
    return <ModelMovie slug={slug}/>
}

export default Movie;